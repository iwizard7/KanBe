# KanBe - Kanban Board Application

Простое и красивое веб-приложение для управления задачами с использованием Kanban доски.

## Возможности

- 🔐 Защита паролем (single-user)
- 📋 Управление колонками (создание, редактирование, удаление)
- ✨ Создание и редактирование задач
- 🎯 Приоритеты для задач (низкий, средний, высокий)
- 🏷️ Тэги для категоризации
- 📅 Дедлайны с визуальными индикаторами
- ☑️ Подзадачи с отслеживанием прогресса
- 🎨 Современный дизайн с темной темой
- 🖱️ Drag & Drop для перемещения задач между колонками
- 💾 Автоматическое сохранение данных

## Установка

### Требования
- Node.js (версия 14 или выше)

### Шаги установки

#### Быстрая установка (через curl)
Если проект уже выложен на GitHub или другой сервер, вы можете запустить установку одной командой:
```bash
bash <(curl -sL https://raw.githubusercontent.com/username/KanBe/main/install.sh)
```

#### Локальная установка из папки
Если вы скачали архив или клонировали репозиторий:
```bash
chmod +x install.sh
./install.sh
```

### Развертывание на Linux сервере

### Вариант 1: Простой запуск

```bash
# Установите Node.js если не установлен
sudo apt update
sudo apt install nodejs npm

# Перейдите в директорию проекта
cd /path/to/KanBe

# Установите зависимости
npm install

# Запустите на порту 3000
npm start
```

### Вариант 2: Запуск с PM2 (рекомендуется для production)

```bash
# Установите PM2
sudo npm install -g pm2

# Запустите приложение
pm2 start server.js --name kanban

# Настройте автозапуск при перезагрузке
pm2 startup
pm2 save

# Просмотр логов
pm2 logs kanban

# Остановка/перезапуск
pm2 stop kanban
pm2 restart kanban
```

### Вариант 3: Использование nginx как reverse proxy

1. Создайте файл `/etc/nginx/sites-available/kanban`:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

2. Активируйте конфигурацию:
```bash
sudo ln -s /etc/nginx/sites-available/kanban /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## Смена пароля

Пароль хранится в файле `data/config.json` в виде bcrypt хеша. Для смены пароля:

1. Остановите сервер
2. Удалите файл `data/config.json`
3. Отредактируйте `server.js` и измените строку с паролем по умолчанию:
```javascript
const defaultPassword = bcrypt.hashSync('ваш_новый_пароль', 10);
```
4. Запустите сервер - будет создан новый config.json с новым паролем
5. Верните обратно 'admin' в коде (опционально)

## Структура проекта

```
KanBe/
├── server.js          # Express сервер и API
├── package.json       # Зависимости проекта
├── data/              # Хранилище данных (создается автоматически)
│   ├── config.json    # Конфигурация и пароль
│   └── board.json     # Данные доски
└── public/            # Frontend файлы
    ├── index.html     # HTML структура
    ├── styles.css     # Стили
    └── app.js         # JavaScript логика
```

## API Endpoints

### Аутентификация
- `POST /api/login` - Вход
- `POST /api/logout` - Выход
- `GET /api/check-auth` - Проверка авторизации

### Доска
- `GET /api/board` - Получить всю доску

### Колонки
- `POST /api/columns` - Создать колонку
- `PUT /api/columns/:id` - Обновить колонку
- `DELETE /api/columns/:id` - Удалить колонку

### Задачи
- `POST /api/columns/:columnId/tasks` - Создать задачу
- `PUT /api/tasks/:taskId` - Обновить задачу
- `DELETE /api/tasks/:taskId` - Удалить задачу
- `POST /api/tasks/:taskId/move` - Переместить задачу в другую колонку

## Резервное копирование

Все данные хранятся в директории `data/`. Для резервного копирования:

```bash
# Создать backup
cp -r data/ data_backup_$(date +%Y%m%d)

# Восстановить из backup
cp -r data_backup_20260126/ data/
```

## Технологии

- **Backend**: Node.js, Express, bcryptjs
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Storage**: JSON файлы
- **Authentication**: Session-based с bcrypt

## Лицензия

MIT
